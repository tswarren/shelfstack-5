<section class="pos-region pos-lines-region" aria-label="Lines">
  <h2>Lines</h2>
  <div class="pos-lines">
    <% if @pos_line_items.any? %>
      <table class="pos-line-table">
        <thead>
          <tr>
            <th scope="col">Description</th>
            <th scope="col" class="num">Qty</th>
            <th scope="col" class="num">Price</th>
            <th scope="col" class="num">Ext</th>
            <th scope="col" class="num">Disc</th>
            <th scope="col" class="num">Tax</th>
            <th scope="col" class="num">Net</th>
          </tr>
        </thead>
        <tbody>
          <%= render partial: "line_item", collection: @pos_line_items, as: :line %>
        </tbody>
      </table>
    <% else %>
      <p class="muted">No lines yet. Scan or search to add the first item.</p>
    <% end %>
  </div>
</section>
