<% content_for :title, "New inventory adjustment" %>

<%= render "shared/page_header", title: "New inventory adjustment",
      breadcrumbs: [ { label: "Inventory adjustments", path: inventory_adjustments_path }, { label: "New" } ] %>

<%= render "form", inventory_adjustment: @inventory_adjustment, reasons: @reasons, variants: @variants %>
