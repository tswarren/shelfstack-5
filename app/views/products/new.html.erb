<% content_for :title, "New product" %>

<%= render "shared/page_header", title: "New product",
      breadcrumbs: [ { label: "Products", path: products_path }, { label: "New product" } ] %>

<%= render "form", product: @product, variant: @variant %>
