<% content_for :title, "Edit #{@product.name}" %>

<%= render "shared/page_header", title: "Edit #{@product.name}",
      breadcrumbs: [ { label: "Products", path: products_path }, { label: @product.name, path: product_path(@product) }, { label: "Edit" } ] %>

<%= render "form", product: @product, variant: @variant %>
