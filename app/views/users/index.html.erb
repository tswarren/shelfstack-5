<h1>Users</h1>
<p><%= link_to "New user", new_user_path if Current.user.can?("administration.user.manage", store: Current.store) %></p>
<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user %></li>
  <% end %>
</ul>
<p><%= link_to "Home", root_path %></p>
