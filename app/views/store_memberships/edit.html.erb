<%= render "shared/page_header", title: "Edit membership",
      breadcrumbs: [ { label: "Store memberships", path: store_memberships_path }, { label: @membership.user.username }, { label: "Edit" } ] %>

<%= render "form", membership: @membership, users: @users, roles: @roles %>
